<!-- 👋 Editing this file? Need help? → https://github.com/framer/api-docs/blob/master/CONTRIBUTING.md -->

import {
  Template,
  CTA,
  Header,
  EmbeddedVideo,
  EmbeddedDemo,
} from "../components"

export default Template

<div>

# Framer API

<span className="lead">
  A complete API reference of the Framer Library, a
  JavaScript library made for rapid interactive prototyping
  for web and mobile.
</span>

</div>

<Header />

<div className="followed-by-list">

## Overview

Framer provides helpers for advanced physics-based animation, complex touch-based gestures and common components for scrolling, paging and interface flows. It’s designed to allow beginners to explore digital product ideas without boundaries. It builds on the following technologies.

</div>

<div>

- **[React](https://reactjs.org)**. A library to define
  components and build interfaces. It provides structure to
  your project by default so you can easily re-use and
  share.

</div>

```tsx
// React component
function Card({ title }) { ... }
```

<div>

- **[JSX](https://reactjs.org/docs/introducing-jsx.html)**. A way to use HTML-like syntax directly inside JavaScript so that you can express document structures in a familiar way.

</div>

```tsx
// JSX markup
<Card title="Hello">World</Card>
```

<div>

- **[TypeScript](https://www.typescriptlang.org/)**. An optional extension to ES6 JavaScript to add types so your editor understands the code you are writing, and can provide excellent inline autocomplete and documentation.

</div>

```tsx
// TypeScript string property type
type CardProps = { title: string }
```

---

## Quick Start

The Framer API was designed to work closely with [Framer X](https://framer.com), but is great as a standalone library. You can install it via [npm](https://www.npmjs.com/), via your Command Line.

```tsx
// Install Framer via command line
npm install framer
```

Once installed, you can import Framer into your files.

```tsx
// Import from Framer
import { Frame, Scroll, useCycle } from "framer"
```

---

### In Framer X

<div>

To learn how to start using the Framer API within Framer X, you can watch this short introductory video tutorial. It covers the following topics.

- How to create new code components.
- Where code components live.
- How to preview code components.
- How previewing works.
- How error handling works.

<CTA
  name="Download Framer X"
  link="https://www.framer.com/"
/>

</div>

<EmbeddedVideo>
    <iframe
      width="560"
      height="315"
      src="https://www.youtube.com/embed/N1vj6C-GIVQ?rel=0"
      frameborder="0"
      allow="accelerometer; autoplay; encrypted-media; gyroscope; picture-in-picture"
      allowfullscreen
    />

</EmbeddedVideo>

---

## Basics

### Animate

<div>

Let’s have a look at a simple animation that scales a `Frame` to half the size. Notice how you can mix `Frame` elements with regular HTML, just like you would do within any React component.

<CTA name="Learn More" link="/api/examples/#animate" />

</div>

```tsx
// Animate scale to 50%
export function MyComponent() {
  return <Frame animate={{ scale: 0.5 }} />
}
```

---

### Cycle

A more extensive example would be to toggle the `scale` value on a tap, which uses the `useCycle` hook to cycle through a set of values.

```tsx
// Cycle scale on tap
export function MyComponent() {
  const [scale, cycle] = useCycle([3, 1])
  return <Frame animate={{ scale: scale }} onTap={cycle} />
}
```

<div>

You can also use `useCycle` to cycle between sets of visual properties.
Here, we’re also rotating the `Frame` on every tap, next to scaling it.

<CTA name="Learn More" link="/api/examples/#cycle" />

</div>

```tsx
// Cycle multiple properties on tap
export function MyComponent() {
  const [twist, cycle] = useCycle([
    { scale: 0.5, rotate: 0 },
    { scale: 1, rotate: 90 },
  ])

  return <Frame animate={twist} onTap={cycle} />
}
```

---

### Dragging

<div>

You can easily make any `Frame` draggble by using the `drag` property.
To constrain dragging to a vertical or horizontal axis, you can include an optional `"x"` or `"y"` value.

<CTA name="Learn More" link="/api/examples/#dragging" />

</div>

```tsx
// Drag Frame in any direction
export function MyComponent() {
  return <Frame drag />
}
```

---

### Scrolling

<div>

For scrolling, you can wrap any content inside a `Scroll` component, which exceeds the size of the component either vertically or horizontally.

<CTA name="Learn More" link="/api/examples/#scrolling" />

</div>

```tsx
// Scroll two Frames
export function MyComponent() {
  return (
    <Scroll>
      <Frame />
      <Frame />
    </Scroll>
  )
}
```
