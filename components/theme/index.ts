import * as React from "react"

export const pill: React.CSSProperties = {
    padding: "24px 50px 26px",
    userSelect: "none",
    borderRadius: 8,
    border: 0,
    outline: 0,
    lineHeight: 1,
    fontSize: 24,
}

export const typeStyle: React.CSSProperties = {
    color: "#aaa",
}

export const propertyStyle: React.CSSProperties = {
    fontSize: 24,
    marginTop: 40,
    marginBottom: 20,
}

export const releaseTagStyle: React.CSSProperties = {
    color: "red",
}

export const propertiesHeaderStyle: React.CSSProperties = {
    marginTop: 80,
    fontWeight: 600,
    marginBottom: 20,
    fontSize: 32,
}

export const baseTextColor = "#333"
export const menuTextColor = "#222"
export const baseHeadingColor = "#111"
